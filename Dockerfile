FROM node:18

WORKDIR /app

COPY . .

RUN yarn 

EXPOSE 3000

ARG DB_USER
ARG DB_PASSWORD
ARG DB_NAME
ARG AWS_REGION
ARG S3_BUCKET

ENV DB_USER=${DB_USER}
ENV DB_NAME=${DB_NAME}
ENV DB_PASSWORD=${DB_PASSWORD}
ENV AWS_REGION=${AWS_REGION}
ENV S3_BUCKET=${S3_BUCKET}


CMD [ "node" , "app.js" ]